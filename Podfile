# Uncomment this line to define a global platform for your project
# platform :ios, '9.0'

target 'SW:LH' do
  # Comment this line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for SW:LH
  pod 'Firebase'
  pod 'Firebase/Core'
  pod 'Firebase/AdMob'
target 'SW:LHTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SW:LHUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
